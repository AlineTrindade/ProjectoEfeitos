Como fazer com que apareça só o nome na PowerShell (e um comando para ver o caminho todo)
Só o nome:
1- Para ver se o "profile" existe ==> Test-Patch $PROFILE

2- Para criar (caso não exista) ==> New-Item -Path $PROFILE -ItemType File -Force

3-Para editar (abrir um ficheiro no bloco de notas) ==> Notepad $PROFILE

4-Guardar no Bloco de notas (PROFILE)
function prompt  {
    $CurrentDir = (Get-Location).Path.Substring((Get-Location).Path.LastIndexOf("\") + 1)
    "$CurrentDir> "
}
Para ver o caminho todo do ficheiro:

1- escrever $PWD